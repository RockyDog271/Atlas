GithubLink = "https://github.com/RockyDog271/Atlas"

async def commandGithub(MessageList, message):
    if len(MessageList) >1
    await message.channel.send(
        f"Here is the GitHub link:"
        f"{GithubLink}"
    )